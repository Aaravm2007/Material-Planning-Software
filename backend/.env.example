# SQLite (default, no setup needed)
DATABASE_URL=sqlite+aiosqlite:///./material_planning.db

# PostgreSQL (switch when ready)
# DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/material_planning
